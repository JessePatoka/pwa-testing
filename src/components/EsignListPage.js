import React from "react";
import { Link } from "react-router-dom";

const EsignListPage = () => (
  <div>
    <h1>Esigns List page</h1>
    <Link to="/productlistpage">ProductListPage</Link>
  </div>
);

export default EsignListPage;
